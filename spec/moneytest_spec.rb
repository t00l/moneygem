require 'spec_helper'

describe Moneytest do
  it 'has a version number' do
    expect(Moneytest::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
